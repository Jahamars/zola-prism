{
  "build": {
    "env": {
      "ZOLA_VERSION": "0.19.2"
    }
  },
  "buildCommand": "curl -sL https://github.com/getzola/zola/releases/download/v${ZOLA_VERSION}/zola-v${ZOLA_VERSION}-x86_64-unknown-linux-gnu.tar.gz | tar zxv && ./zola build",
  "outputDirectory": "public",
  "installCommand": "echo 'No install needed'"
}
